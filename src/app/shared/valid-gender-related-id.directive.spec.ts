import { ValidGenderRelatedIdDirective } from './valid-gender-related-id.directive';

describe('ValidGenderRelatedIdDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidGenderRelatedIdDirective();
    expect(directive).toBeTruthy();
  });
});
