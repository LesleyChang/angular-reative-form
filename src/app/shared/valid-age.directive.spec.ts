import { ValidAgeDirective } from './valid-age.directive';

describe('ValidAgeDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidAgeDirective();
    expect(directive).toBeTruthy();
  });
});
